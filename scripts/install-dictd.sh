#!/bin/bash

# This script installs the dictd and dict (network dictionary service and client)
# and a local database for comprehansive english dictionary.

sudo apt install dictd
sudo apt install dict-gcide
