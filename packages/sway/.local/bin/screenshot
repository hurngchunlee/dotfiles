#!/bin/bash

dir=$HOME/tmp/screenshots
mkdir -p $dir

output="$dir/screenshot-$(date +%FT%T).png"
grim -g "$(slurp -c 960000)" - | swappy -o $output -f - && notify-send -c screenshot "$(basename ${output}) created"
