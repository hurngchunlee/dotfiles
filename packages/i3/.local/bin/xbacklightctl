#!/bin/bash

msgId='1000'

dunstify="dunstify -r ${msgId} -a xbacklightctl"

case $1 in
inc)
    xbacklight -inc 15 && ${dunstify} "🔆 $(printf '%.0f' $(xbacklight -get))"
    ;;
dec)
    xbacklight -dec 15 && ${dunstify} "🔆 $(printf '%.0f' $(xbacklight -get))"
    ;;
esac
