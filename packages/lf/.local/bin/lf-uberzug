#!/bin/sh
################################################################################
##
## Start `lf` with this script instead of the `lf` binary to get image preview
## using Überzug.
##
## This script is copied from the lf wiki:
##
##  - https://github.com/gokcehan/lf/wiki/Previews#with-%C3%9Cberzug
##
## It makes use of $HOME/.cache/lf for preview thumbnails.
##
################################################################################
set -e

if [ -n "$DISPLAY" ]; then
  export FIFO_UEBERZUG="${TMPDIR:-/tmp}/lf-ueberzug-$$"

  cleanup() {
    exec 3>&-
    rm "$FIFO_UEBERZUG"
  }

  mkfifo "$FIFO_UEBERZUG"
  ueberzug layer -s <"$FIFO_UEBERZUG" &
  exec 3>"$FIFO_UEBERZUG"
  trap cleanup EXIT

  if ! [ -d "$HOME/.cache/lf" ]; then
    mkdir -p "$HOME/.cache/lf"
  fi

  lf "$@" 3>&-
else
  exec lf "$@"
fi
